# Text Metrics Demo

This small package normalizes prose and reports word statistics. It is used as
a repeatable coding-agent demo: the template intentionally contains a bug, so
copy it before asking the agent to repair the failing tests.

Run the tests with:

```bash
python -m pytest -q
```

